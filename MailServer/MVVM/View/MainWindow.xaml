<Window x:Class="MailServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MailServer"
        mc:Ignorable="d"
        Title="Email Server" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <ListBox Name="AccountList" Grid.Column="0" Margin="10" SelectionChanged="AccountList_SelectionChanged"/>
        <ListBox Name="EmailList" Grid.Column="1" Margin="10" SelectionChanged="EmailList_SelectionChanged"/>

        <StackPanel Grid.Column="2" Margin="10">
            <TextBlock Text="Email Content:" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBox Name="EmailContent" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="200" IsReadOnly="True"/>

            <Button Content="Refresh" Click="RefreshButton_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
